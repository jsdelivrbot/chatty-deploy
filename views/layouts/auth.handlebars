<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="theme-color" content="#667AFA" />
    <title>{{ appName }}</title>
    {{> _style }}
    {{> _script }}
</head>

<body class="bg-light">
    <div class="bg-overlay"></div>
    <div class="auth bg-transparent">
        <header class="text-center p-4">
            <h2 class="m-0 logo">ChatTy</h2>
        </header>

        <div class="container mt-2">
            <div class="row">
                <div class="col-lg-5 col-md-6 mx-auto" id="app">
                </div>
            </div>
        </div>
        <footer class="footer text-center p-2 mt-5">
            <a href="#" class="text-secondary small">@_dedeardiansya</a>
        </footer>
    </div>
    
    {{> includes/_login }}
    {{> includes/_register }}
    
    {{{ body }}}
</body>
</html>
